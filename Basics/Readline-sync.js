const readTheInput = require(readline-sync);
const nowItTakesInput = question.readTheInput("There you can write your input");


//Before this process, we have to download the readline-sync module inside the node.js
// -->1. npm i readline-sync
// -->2. node<the name of the js file>
